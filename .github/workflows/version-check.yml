name: Version Pin Check

on:
  pull_request:
  push:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Verify pinned package versions
        uses: ANcpLua/ANcpLua.NET.Sdk/.github/actions/version-pin-check@actions-version-pin-check-v1
