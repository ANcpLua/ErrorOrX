name: Version Pin Check

on:
  pull_request:
  push:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Verify pinned package versions
        run: pwsh ./eng/verify-published-versions.ps1
